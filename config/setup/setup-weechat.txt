sudo port install weechat +aspell +debug +doc +lua +perl +python +ruby +scheme +tls

# Do the symlinks shit

# Install pip if it doesn't exist
sudo easy_install pip
sudo pip install pync

# Install notifications stuff.
cd .weechat/python/autoload/
git clone git@github.com:sindresorhus/weechat-notification-center.git

