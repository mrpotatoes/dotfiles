#!/bin/sh
# Open an application

# The help text if needed.
if [[ $1 == "--help" ]]
	then
	  manuals 'oo'
	  exit 0;
fi

# Actual functionality.
if [ -n "$1" ]
	then
	  APP=$@
    open -a "${APP}.app"
  else
    open .
fi